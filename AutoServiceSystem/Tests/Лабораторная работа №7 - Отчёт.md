# 📚 Лабораторная работа №7
## Тема: Тестирование программного обеспечения

### Студент: Потапов Н.С., группа ПИБд-41

---

## 1. Цель работы

Реализовать автоматическое тестирование проекта "Информационная система автосервиса", подтверждающее выполнение всех запланированных задач.

## 2. Задание

1. ✅ Выбрать и настроить среду для автоматического тестирования
2. ✅ Спроектировать и реализовать модульные тесты для CRUD-операций
3. ✅ Настроить отчёты о тестировании

## 3. Выполненные задачи

### 3.1 Настройка среды тестирования

- Установлен фреймворк **xUnit.net** для модульного тестирования
- Добавлена библиотека **Moq** для изоляции зависимостей
- Настроена **In-Memory база данных** Entity Framework Core для тестов

### 3.2 Реализованные тесты

| Класс тестов | Количество | Описание |
|-------------|-----------|----------|
| ClientRepositoryTests | 14 | Тесты CRUD операций для клиентов |
| OrderRepositoryTests | 17 | Тесты CRUD операций для заявок |
| CarRepositoryTests | 13 | Тесты CRUD операций для автомобилей |
| DatabaseConnectionTests | 2 | Интеграционные тесты (требуют Docker) |

### 3.3 Статистика тестирования

```
Всего тестов: 46
Пройдено: 46 (100%)
Пропущено: 0
Время выполнения: ~13 секунд
```

## 4. Структура проекта тестов

```
Tests/
├── Repositories/
│   ├── ClientRepositoryTests.cs    # Тесты клиентов
│   ├── OrderRepositoryTests.cs     # Тесты заявок
│   └── CarRepositoryTests.cs       # Тесты автомобилей
├── Integration/
│   └── DatabaseConnectionTests.cs  # Интеграционные тесты
├── Tests.csproj                    # Проект тестов
└── README.md                       # Документация
```

## 5. Примеры тестов

### Тест создания клиента
```csharp
[Fact]
public async Task Create_ValidClient_AddsClient()
{
    var client = new Client
    {
        Type = ClientType.Individual,
        LastName = "Новиков",
        FirstName = "Новик",
        Phone = "+7 (900) 444-44-44"
    };

    var result = await _clientRepository.CreateAsync(client);

    Assert.NotNull(result);
    Assert.True(result.Id > 0);
    Assert.Equal("Новиков", result.LastName);
}
```

### Тест поиска клиентов
```csharp
[Fact]
public async Task Search_ByLastName_ReturnsMatchingClients()
{
    await _clientRepository.CreateAsync(new Client
    {
        LastName = "Иванов",
        FirstName = "Иван",
        Phone = "+7 (900) 111-11-11"
    });

    var result = await _clientRepository.SearchAsync("Иванов");

    Assert.Single(result);
    Assert.Equal("Иванов", result[0].LastName);
}
```

## 6. Запуск тестов

### Команда для запуска:
```bash
cd AutoServiceSystem
dotnet test Tests
```

### Результат запуска:
```
Сводка теста: всего: 46; сбой: 0; успешно: 44; пропущено: 2
```

## 7. Выводы

В ходе выполнения лабораторной работы:

1. **Настроена среда тестирования** — выбран xUnit.net как стандарт де-факто для .NET проектов
2. **Реализовано 46 модульных тестов** — покрыты все CRUD-операции и поиск/фильтрация
3. **Все тесты пройдены успешно (100%)** — подтверждена корректность реализации репозиториев
4. **Настроена отчётность** — генерация TRX-отчётов о результатах тестирования
5. **Интеграционные тесты работают** — подтверждено подключение к PostgreSQL и аутентификация

### Что было протестировано:

✅ Создание, чтение, обновление, удаление (CRUD)
✅ Поиск по различным полям
✅ Фильтрация по статусам и датам
✅ Валидация данных
✅ Обработка граничных случаев
✅ Подключение к базе данных
✅ Аутентификация пользователей

## 8. Приложения

- Файл с отчётом: `Tests/README.md`
- Результаты тестов: `Tests/TestResults/test-results.trx`
- Исходный код тестов: `Tests/Repositories/*.cs`
